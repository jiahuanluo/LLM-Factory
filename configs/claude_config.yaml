# =============================================================================
# Claude Code 一键配置
# 用法: python setup_claude.py configs/claude_config.yaml
# 选项: --dry-run（仅打印） --skip-plugins（跳过插件安装）
# =============================================================================

# API 配置
api_base_url: "http://100.103.98.108:11288/efllmas-agw/anthropicproxy/"  # API 地址
api_key: "sk-zhtsf_dss_ide_cib_common_12371180_31_a8b5ae36"                            # API Key（必填）

# 模型配置
model: "glm-4.7-cib"                              # 默认模型

# 超时配置
api_timeout_ms: 3000000                                 # API 超时时间（毫秒）

# 插件配置（可选，离线部署用）
plugins_enabled: true                                   # 是否安装插件
plugins_dir: "deploy/plugins"                           # 插件源目录（相对于仓库根目录）
