# Python
__pycache__/
*.py[cod]
*$py.class
*.so

# Jupyter
.ipynb_checkpoints/

# 训练产物与数据
output/
data/
tmp/

# 训练 checkpoint 目录（权重靠 *.safetensors 等兜底规则忽略，
# 但这些目录里的 config 是训练产物，整体继续忽略）
models/gte-new-pretrained/
models/gte-old-pretrained/
models/gte-new-scratch/
models/gte-old-scratch/

# 兜底：误放的权重 / 缓存文件
*.safetensors
*.bin
*.onnx
*.ckpt
*.pt
*.pth

# OMC / 工具状态
.omc/
models/.omc/

# Claude Code 插件：本地 git 仓库和运行时状态（ZIP 分发用真实文件）
deploy/plugins/**/.git/
deploy/plugins/**/.in_use/
deploy/plugins/**/.omc/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
